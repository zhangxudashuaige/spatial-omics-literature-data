# Jupyter 使用说明

双击 `launch_jupyter.cmd` 会从总仓库根目录启动 Jupyter Lab，并打开数据清单 Notebook。

如果提示找不到 Jupyter，在 PowerShell 中运行：

```powershell
python -m pip install -r papers\cheng-2024-mouse-embryo-3d\notebooks\requirements.txt
```

当前 Notebook 使用合成小样本，只演示字段和表连接，不代表论文结果。
