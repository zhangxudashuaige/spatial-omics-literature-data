# 小型示例数据

当前没有提交来源于 GSE278603 的样例 H5AD。

原因：GEO 页面没有单独列出允许再分发衍生子集的许可证。网络恢复后可用 `scripts/create_sample_data.py` 从最小的 E7.5 rep2 抽取 500–1000 个观测，但只有在明确确认数据许可、文件小于 10 MB 且 README 标注来源后，才应将其加入 GitHub。

测试脚本可以在 `data/processed/` 生成完全合成、会被 Git 忽略的 H5AD；它不属于论文数据。
