# 结果目录规则

只提交小型测试产生的 `domain_labels.csv`、`domain_probabilities.csv`、`svg_results.csv`、`meta_gene_results.csv`、`metrics.json` 和低分辨率示意图。完整邻接矩阵、H5AD、模型中间对象、论文完整结果及来源不明文件放入 `results/full/`，由 `.gitignore` 排除。
