# HEIST 小样例占位目录

当前没有提交生物学样例：官方项目尚未给出一个同时具备精确来源、固定版本和明确再分发许可的小型数据包。

将来只有在 manifest 补齐来源 URL、版本、文件大小、SHA256 和许可后，才可把不含敏感信息且体积合理的样例放到这里。`.pt`、`.h5ad` 和 `.csv` 即使位于本目录也默认被 `.gitignore` 排除，需人工审核后再为具体文件添加例外。
